{{>layout/header}}

<div id="wrapper">
    <!-- Nav -->
    <nav id="nav">
        <a href="#" class="icon solid fa-home"><span>Home</span></a>
        <a href="#manual" class="icon solid fa-envelope"><span>Manual</span></a>
        <a href="#analyze" class="icon solid fa-folder"><span>Analyze</span></a>
        <a href="#post" class="icon brands fa-twitter"><span>Post</span></a>

    </nav>

    <!-- Main -->
    <div id="main">

        <!-- Me -->
        <article id="home" class="panel intro">
            <header>
                <h1>민원 분석기</h1>
                <p>
                {{#userName}}
                    Logged in as: <span id="user">{{userName}}</span>
                    <a href="/logout" class="btn btn-info active" role="button">Logout</a>
                {{/userName}}
                {{^userName}}
                    <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
                    <a href="/oauth2/authorization/naver" class="btn btn-secondary active" role="button">Naver Login</a>
                {{/userName}}
                </p>
            </header>
            <a href="#manual" class="jumplink pic">
                <span class="arrow icon solid fa-chevron-right"><span>See manual</span></span>
                <img src="images/me.jpg" alt="" />
            </a>
        </article>

        <!-- Manual -->
        <article id="manual" class="panel">
            <div>
                <center>
                    <h2>사용법</h2>
                    <p>민원 분석기를 사용하기 위해서는 기존 엑셀 파일을 </p>
                    <p>"민원제목", "대분류", "소분류", "민원요지"를 제외하고</p>
                    <p>다른 데이터들을 제거한 뒤에 A열 1행에 데이터들을</p>
                    <p>옮긴 뒤에 민원 분석을 시작합니다</p>
                </center>
            </div>
            <div>
                <center>
                    <h4>기존 엑셀 파일</h4>
                    <img src="images/before.png" width="800px"  alt="데이터 정제 전 엑셀" />
                </center>
            </div>
            <div>
                <center>
                    <h4>데이터 정제 후 엑셀 파일</h4>
                    <img src="images/after.png" height="600px" alt="데이터 정제 후 엑셀" />
                </center>
            </div>
        </article>

        <!-- Analyze -->
        <article id="analyze" class="panel">
            <section id="first" class="main special">
                <header class="major">
                    <h2>민원 분석기</h2>
                </header>
                <div>
                    <form method="POST" enctype="multipart/form-data" action="/python_model">
                        <h5>한 개의 파일만 가능합니다.</h5>
                        <input type="file" name="file"/>
                        <input type="submit" value="분석하기"/>
                    </form>
                </div>
            </section>
        </article>

        <!-- Post -->
        <article id="post" class="panel">
            <a href="/posts/save" role="button" class="btn btn-primary">글 등록</a>
            <!-- 목록 출력 영역 -->
            <table class="table table-horizontal table-bordered">
                <thead class="thead-strong">
                <tr>
                    <th>게시글번호</th>
                    <th>제목</th>
                    <th>작성자</th>
                    <th>최종수정일</th>
                </tr>
                </thead>
                <tbody id="tbody">
                {{#posts}}
                    <tr>
                        <td>{{id}}</td>
                        <td><a href="/posts/update/{{id}}">{{title}}</a></td>
                        <td>{{author}}</td>
                        <td>{{modifiedDate}}</td>
                    </tr>
                {{/posts}}
                </tbody>
            </table>
        </article>

    </div>

</div>

{{>layout/footer}}